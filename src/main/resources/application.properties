# When the debug mode is enabled, a selection of core loggers (embedded container, Hibernate and Spring Boot) are configured to output more information.
# http://docs.spring.io/spring-boot/docs/current/reference/html/boot-features-logging.html#boot-features-custom-log-levels
#debug=true
#
# In order to minimize upgrade pain, we set hibernate.id.new_generator_mappings to false for Hibernate 5. The Hibernate
# team generally don�t recommend this setting, so if you�re happy to deal with generator changes, you might want to set
# spring.jpa.hibernate.use-new-id-generator-mappings to true in your application.properties file.
# https://github.com/spring-projects/spring-boot/wiki/Spring-Boot-1.4-Release-Notes#generator-mappings
spring.jpa.hibernate.use-new-id-generator-mappings=true
#
# JSON pretty print
spring.jackson.serialization.indent_output=true
