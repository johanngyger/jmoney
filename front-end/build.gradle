plugins {
  id "com.moowork.node" version "1.0.1"
}

node {
  version = '6.9.4'
  npmVersion = '3.10.10'
  download = true
}

clean {
  delete "dist"
}

task ngBuild(type: NpmTask) {
  args = ['run', 'ngbuildprod']
}
ngBuild.dependsOn npmInstall

build.dependsOn ngBuild
